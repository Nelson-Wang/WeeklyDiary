第2周
刚来公司实习，组里还没有布置具体的工作，本周的主要内容是学习Ruby语言和TCL语言。Ruby是一种真正的面向对象编程语言，可扩展性强，用于Ruby编写的大程序用于维护。Ruby语言的主要内容有Ruby的数据类型、类和对象、模块、循环结构、多线程、文件的输入与输出和异常处理。其中类和对象是Ruby的主要内容，Ruby的多线程比较困难，经过多次查阅文献才最终弄懂多线程的原理和使用方法。TCL是一种通用的脚本语言，包含了两个部分：一个语言和一个库。语言部分主要使用于发布命令给一些互交程序如文本编辑器、调试器和Shell，TCL库包含了一个分析器、用于执行内建命令的例程和可以使你扩充（定义新的过程）的库函数。
最后，在周五下午，我们进行了这一周的学习汇报，组内老师对我们的学习汇报进行了点评，指出我们学习中的遗漏之处，并给我们提出了一些问题留给我们进行思考，这种学习交流方式让我们对学习的只是有了更深的理解。
第3周
实习来到了第三周，本周的主要工作是业务学习和配置ONT上线。学习了交换机转发原理和802.1Q协议，熟悉了简单测试环境组网搭建；此外，学习了二层转发流程，熟悉了二层报文格式和二层协议。配置ONT上线时，首先配置DBA模版；接着配置线路模版，包括建立TCONT并映射到DBA、建立GemPort并映射到TCONT和建立GemPort与ONT侧业务的映射使得gem可以通过vlan或指定端口转发；然后配置置业务模板，包括配置ONT以太端口的能力集和设置ONT端口vlan；最后ONT上线，包括使能ONT自动查找和端口ONT确认。
在进行ONT上线的过程中，由于中途下电了，所以在第二次上线时出现了忘记使能的状况，只有完全掌握每个语句的逻辑，才不会在第二次上线的时候出现这种低级的错误问题，所谓的粗心，很多时候是因为基础不够扎实。相信经过学习，对业务的掌握情况将会更好。
第4周
本周是来华为公司实习的第四周，组内分配给我一个任务：使用TCL语言写一个脚本，目标是可以自动的发送5122-XGPON环境的所有命令，检测该环境是否可以正常执行而不存在卡死的现象。刚开始拿到这个任务时完全不知道怎么去做，经过和组内的老师交流沟通后，有了大致的解决思路。我选择使用列表的数据结构，将要遍历的命令放入列表中，当命令遍历完以后，将命令从列表的集合中取出。目前代码的整体代码结构已经给出，但是在执行测试的过程中遇到大量问题，比如命令行返回值时间无法确定、遇到某些命令行卡死等等问题。
本周刚开始进行工程编码工作，过程中遇到了各种各样的问题，使得我的工作进度也比较缓慢，希望接下来的工作效率可以得到提升。
第5周
本周的工作是继续完善上周布置的脚本任务，针对命令行返回值时间无法确定的问题，我没隔一段时间向环境发送“Enter”命令，当返回值只收到“>USER”时，表示命令行的返回值返回完成，此时即为命令行返回值的时间。遇到卡死环境的命令行，我编写代码自动向环境中发送“quit” 、“y” 和“combo.gdb”，使得环境可以自动恢复，然后继续进行命令行的遍历。此外，当遇到“Unknown command”时，表明发送的指令是无效的指令，记录出现的指令，将这些指令放到有问题的指令集中。
经过一周的努力，脚本的基本任务已经实现，经过测试脚本也可以跑得通，下周组内老师将对我们的代码进行评审，检查我们代码的功能实现情况。
第6周
本周一组内老师对我们的代码进行第一次评审，我们现场展示了代码的功能，对组内老师的提问一一进行了解答，老师对我们的工作进行了肯定，并对我们的代码提出了新的要求，主要包括以下内容：支持根据策略选择覆盖最大值、最小值、默认值及任意组合；支持配置需要覆盖和不需要覆盖的命令字头组合；支持测试结果保存到Log，Log区分正常命令行和异常命令行；支持识别命令行导致系统复位、挂死场景，能够自动恢复继续测试；支持Error命令行记录和人工确认命令行正确性；支持异常参数测试（如超出参数取值范围），预期返回命令行执行错误，并且不会导致复位等异常，否则记录为错误Log；支持范围型参数除最大值、最小值、默认值外选择随机一个参数。
     组内老师评比后，我根据老师提的各种问题，对代码进行修改和完善，基本所有的要求都得到了实现，但是支持异常参数测试和支持配置需要覆盖的命令字头组合这两个需求还没有实现，需要下周继续进行。
第7周
本周的主要任务是对脚本工作进行收尾，实现了支持异常参数测试和支持配置需要覆盖的命令字头组合这两个需求。经过测试，脚本能够正常的实现所需要的功能，最后按照代码的书写规范，我对代码进行了最后的修改。经过老师的第二次评审，脚本达到了老师的要求。
经过这次脚本的工作，我对这像工作有了更多的认识，明白了如何进行脚本的调试，能够通过调试结果不断的修改优化代码，当然，目前我写的代码的冗余度和可读性还有很大的改善空间，需要在以后的工作中继续完善。除此之外，我也明白了工作时交流沟通的重要性，通过与同事和上司交流，可以发现自己工作的不足之处，更快的解决工作中遇到的问题。
第8周
本周是实习的第8周，这周我们第一次来到了实验室，进行环境的搭建。李泽和梁中原详细向我们讲解了如何搭建环境，并解释了每个设施和连接线的作用。
开始接触时比较困难，经常听过以后没多久就忘记了之前老师们讲解的内容，还好当老师发现我们不会做或者做错的时候及时进行指导改正。
几天的实验室工作还是比价辛苦的，特别是每个环境都连接了很多光纤，需要将每个不要的光纤拔下来，然后插入需要的光纤，还要将光模块插到合适的位置上去。有一次插光模块时发现所有的卡槽都使用完了，而光模块还剩下一个，检查了好久才发现有一个之前环境的光模块没有拔出来，从而造成了多了一个光模块。可见，工作时需要认真仔细，有时一个不小心可能会造成很大的问题，让我们不得不花费更多的时间去解决这个问题。
第9周
    经过几周的忙碌，我迎来了一个比较轻松的工作周，这周组里没有安排具体的工作，我的主要任务是学习Python语言。Python 是一个高层次的结合了解释性、编译性、互动性和面向对象的脚本语言，拥有可移植、可扩展和易于维护的特点。学习的内容包括Python基本数据类型、循环结构、迭代器、生成器、函数、内存结构、模块、文件的读写、错误、异常和面向对象的编程。学习的过程
中遇到一些问题，对迭代器和生成器理解不深，不会使用迭代器和生成器去解决问题，除此之外，对浅拷贝和深拷贝的差别也没弄明白。在周五下午的学习汇报时，我像老师们问了这些问题，老师们对这些问题一一进行了解答，这些解答让我收获很大。
通过本周的学习，我对Python的基础知识有了更深的了解，希望接下来可以对Python进行深入学习。
第10周
本周是来到华为实习的第10周，组内老师向我们布置了一个新的任务——搭建一个Web网站，网站的目的是对用例测试结果进行图形化展示。而我的任务是进行网站的数据库管理和网站的部署。对于数据库我使用的SQLite，SQLite是一个进程内的库，实现了自给自足的、无服务器的、零配置的、事务性的 SQL 数据库引擎，使用SQLite时不需要在系统中配置，可以直接访问其存储文件。网站部署使用uWSGI + Nginx + 浏览器 + 服务器的架构，工作时浏览器发起请求，Nginx接受请求，如果是静态文件，如html、js、css、png等则读取磁盘并返回给浏览器，如果不是静态文件，则将所有请求信息转发给uWSGI，uWSGI接收请求，进行url匹配找到请求的视图函数，调用执行，返回响应给Nginx，再返回给浏览器。
经过一周的努力，SQLite、uWSGI和Nginx的安装已经完成，但是目前Nginx的配置出现的错误，启动Nginx时无法起作用，这个问题留给下周继续解决。
第11周
     本周继续解决上周网站部署时的遗留问题，经过查询大量资料后，我找到了Nginx配置失败的原因，即Nginx中的“location /static”配置错误，对其进行重新配置后，Nginx启动后可以正常发挥作用。完成这些配置后，我将组内两位同学的Python后端代码、前端的相关代码和我的数据库代码打包发到欧拉系统中，然后配置相关的uWSGI和Nginx。完成这些配置后，在浏览器输入“100.100.228.240:8001”可以登录到搭建的网站首页，但是却发现里面的一些js动作指令无法正常使用，经过检查后发现是因为js的跳转指令是指向本地的ip地址，修改为本地文件后，js的跳转指令可以正常使用。
目前的网站已经可以正常访问，但是现在只能通过ip地址访问，下周的目标是用域名代替ip地址访问并完善网站的具体功能。
第12周
本周是实习的第12周，本周的主要任务是域名代替ip地址访问和完善搭建的网站。域名，又称网域，是由一串用点分隔的名字组成的Internet上某一台计算机或计算机组的名称，用于在数据传输时对计算机的定位标识。目前比较出名的域名有阿里云和腾讯云，刚开始时还打算在阿里云购买一个域名，后来从黎标老师那里了解到，公司里不能使用其他公司的域名，并且公司里有华为云，完全可以满足在公司内网中进行访问，最后我在公司的域名系统中申请了一个域名，
配置网站的访问地址为申请的域名。此外，针对网站的数据库系统进行了改善，使得数据库的的冗余性得到了改善，并提高了数据库数据的安全性。
    在周五下午，组内老师们对搭建的网站进行了评审，提出了修改意见和需要改善的地方，同时也对我们的工作做出了肯定。
第13周
本周的工作比较轻松，前两天我对之前的网站进行了最后的收尾工作，剩下的时间主要进行学习来充实自己。学习的内容主要包括两个方面：一是Python正则表达式、进程和线程；二是IP路由选择和静态路由的配置。Python正则表达式贪婪匹配和非贪婪匹配理解比较困难，花费了很长时间才弄懂贪婪匹配和非贪婪匹配的关键；此外，Python线程中的生产者和消费者的问题也比较难理解，生产者消费者的问题是一直生产，一直消费，中间有阈值，解决这类问题的关键是避免供求关系不平衡而导致出现问题。IP路由选择是逐跳进行的，主要完成三个功能： 一，搜索路由表寻找能于目的IP地址完全匹配的表目。找到则把报文发送给该表目指定的下一站路由器或直接连接的网络接口；二，搜索路由表寻找能与目的网络号相匹配的表目。找到则把报文发送给该表目指定的下一站路由器或直接连接的网络接口；三， 搜索路由表寻找标为默认的表目。
通过本周的学习，对 Python正则表达式、进程和线程等内容有了更深的了解，同时对IP路由选择的相关内容有了更多的了解。
第14周
本周是实习的第14周，本周的主要任务是使用Testgine测试仪来观测接收报文的详细情况。这是我第一次系统的使用Tesgine测试仪收发包工具，Tesgine是华为公司具有自主知识产权的宽带通信测试仪器，它为公司内具备数据业务接口的产品，提供测试数据生成、数据分析、协议仿真、性能测试、自动化测试等业务支撑。Tesgine内部附带的wireshark插件将报文抓取捕获后自动解析报文格式得以看见该报文的全貌，非常直观。
本周使用Testgine发包时发现并解决了几个问题，一，Testgine使用光模块的时候端口是UP的但是业务不通，这是由于插上光模块之后，Testgine默认不自协商，必须要在Physical Setup这里手工设置为自动协商模式；二，Testgine发送报文长度和预期不符，这块由于帧格式中有4字节的CRC，因此需要在自定义的长度加4；三，Testgine测试时有杂包，这块需要在Link Setup把自动发送的ARP以及IPV6发现报文去掉。

